env: dev

kafka:
  bootstrap_servers: "localhost:9092"
  topic: "events"

paths:
  bronze: ""
  silver: "s3a://silver/events/"
  gold: "s3a://gold/results/"
  checkpoint: "/tmp/checkpoint"

streaming:
  watermark: "1 minute"
  window: "1 minute"

spark:
  app_name: "adtech-pipeline"
  checkpoint: "/tmp/checkpoint"

minio:
  endpoint: "http://localhost:9000"
  access_key: "minio"
  secret_key: "minio123"
